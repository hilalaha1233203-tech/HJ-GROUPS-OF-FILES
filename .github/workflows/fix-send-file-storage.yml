name: Legacy storage delivery patch (manual)

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  legacy:
    runs-on: ubuntu-latest
    steps:
      - run: echo "Legacy auto-patch disabled. Source is maintained directly on main."