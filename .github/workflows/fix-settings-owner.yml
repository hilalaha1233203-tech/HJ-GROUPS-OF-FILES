name: Fix settings owner authorization

on:
  workflow_dispatch:

permissions:
  contents: write

jobs:
  fix:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0
      - name: Historical settings-owner patch
        run: echo "Manual-only legacy patch workflow."
