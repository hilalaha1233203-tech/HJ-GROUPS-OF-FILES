name: Remove Force Subscribe Completely

on:
  workflow_dispatch:

permissions:
  contents: write

jobs:
  patch:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Historical force-sub patch
        run: echo "Manual-only legacy patch workflow."
