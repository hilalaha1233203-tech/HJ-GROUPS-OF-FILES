name: Storage recovery finalizer

on:
  workflow_dispatch:

permissions:
  contents: write

jobs:
  finalize:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0
      - name: Historical storage recovery patch
        run: echo "Manual-only legacy patch workflow."
